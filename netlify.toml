[build]
  command = "pip install -r requirements.txt && mkdocs build"
  publish = "site"

[build.environment]
  PYTHON_VERSION = "3.11"

[[plugins]]
  package = "@netlify/plugin-sitemap"

# Optional: redirect root to latest docs page
# [[redirects]]
#   from = "/"
#   to = "/"
#   status = 200
